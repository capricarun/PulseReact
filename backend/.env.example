PORT=4000
CORS_ORIGIN=http://localhost:5173

DB_HOST=localhost
DB_PORT=3306
DB_USER=pulse_app
DB_PASSWORD=changeme
DB_NAME=trending_app

# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace_with_a_long_random_string

# Number of reverse proxies in front of the API (0 = none, 1 = nginx, 2 = ALB + nginx on EKS)
TRUST_PROXY_HOPS=0
