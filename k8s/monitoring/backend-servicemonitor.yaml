# Tells Prometheus to scrape the backend's /metrics endpoint (application metrics) every 30 seconds.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pulse-backend
  namespace: pulse
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: pulse
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: backend
  namespaceSelector:
    matchNames: ["pulse"]
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
